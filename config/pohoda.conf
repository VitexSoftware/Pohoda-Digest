# Pohoda-Digest Configuration
# Copy this file to /etc/pohoda-digest/pohoda.conf and customize

# Pohoda mServer connection settings
POHODA_HOST=localhost
POHODA_PORT=5435
POHODA_DATABASE=StwPh_12345678_2024

# Authentication (if required)
POHODA_USERNAME=
POHODA_PASSWORD=

# Report settings
REPORT_THEME=bootstrap
OUTPUT_DIR=/var/cache/pohoda-digest
LOG_LEVEL=info

# Email settings (for email theme)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=noreply@example.com
TO_EMAIL=admin@example.com

# Module configuration
MODULES=outcoming_invoices,debtors
CACHE_TTL=3600